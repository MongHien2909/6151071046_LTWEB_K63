@model IEnumerable<WebQLSach_TH.Models.NHAXUATBAN>

@foreach (var item in Model)
{
    <a href="@Url.Action("SPTheoNBX", "Bookstore", new { id = item.MaNXB })" class="list-group-item">
        @item.TenNXB
    </a>

}
